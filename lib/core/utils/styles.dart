import 'package:flutter/material.dart';

import 'colors.dart';

class AppStyles {
  static const itemSectionTitle = TextStyle(
    fontSize: 18,
    fontWeight: FontWeight.bold,
  );

  static const featureTitle = TextStyle(
    fontWeight: FontWeight.bold,
    fontSize: 10,
    color: AppColors.mainGreen,
    height: 1,
  );
}
